define([
    'jquery', 
    'underscore', 
    'backbone',
    'bootstrap',
], function($, _, Backbone, bootstrap) {
    
	var ProjectModel = Backbone.Model.extend({

	});

	return ProjectModel;
    
});
